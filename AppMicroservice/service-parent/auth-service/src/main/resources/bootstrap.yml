server:
  port: 8081

spring:
  cloud:
    config:
      username: admin
      password: secret
  application:
    name: auth-service
  config:
    import: optional:http://localhost:8888/

CONFIRMATION_TOKEN_EXP_TIME: 15



