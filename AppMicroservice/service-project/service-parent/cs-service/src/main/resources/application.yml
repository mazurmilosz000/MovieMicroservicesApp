server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mazurmilosz000/MovieServiceAppConfig
        default-label: master
  security:
    user:
      name: admin
      password: secret